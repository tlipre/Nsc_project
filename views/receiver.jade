html
  head
  body
    textarea(id="editor" cols="100" rows="20" disabled)
    script(src="https://cdn.socket.io/socket.io-1.3.3.js")
    script(src="https://code.jquery.com/jquery-2.1.3.min.js")
    script.
      socket = io();
      socket.on('broadcast', function(data){
        console.log(data)
        $("#editor").val(data.word)
      });